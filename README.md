# aero-store
A lightweight, zero-dependency e-commerce storefront template designed with modern web aesthetics. Built purely using semantic HTML, vanilla CSS (Flexbox/Grid), and ES6+ JavaScript.
echo "# aero-store" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/prashannapr56-byte/aero-store.git
git push -u origin main
